# DreamChamber Python server dependencies
# Install: pip install -r requirements.txt
#
# Minimum required (always install):
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
numpy>=1.24.0
pydantic>=2.0.0

# ── ASR backends (install one) ─────────────────────────────────────────────
# Moonshine v2 (recommended — fast, local, ~40MB):
#   pip install moonshine-onnx
#
# Whisper (battle-tested, more models):
#   pip install openai-whisper
#
# Both work as fallback. moonshine-onnx is preferred on Apple Silicon.
moonshine-onnx>=0.2.0

# ── TTS backends (install one) ─────────────────────────────────────────────
# Kokoro-82M (recommended — best quality/size ratio, ~82MB):
#   pip install kokoro>=0.9.4 soundfile sounddevice
#
# Dia-1.6B (dialogue TTS — richer, ~1.6GB):
#   pip install git+https://github.com/nari-labs/dia.git soundfile sounddevice torch
#
# System fallback: uses macOS 'say' — zero extra deps
kokoro>=0.9.4
soundfile>=0.12.0
sounddevice>=0.4.7

# ── Optional: Dia-1.6B (uncomment for dialogue TTS) ─────────────────────────
# torch>=2.0.0
# dia @ git+https://github.com/nari-labs/dia.git
