{
  "name": "media-gateway",
  "version": "1.0.0",
  "description": "NOIZY Media Gateway — Consent-gated audio/video streaming from R2",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
