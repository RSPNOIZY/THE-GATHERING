**Quiz — Windows + Storage (SMB/Performance)**

1) Command to display MTU settings in Windows?
2) What NIC setting can impact jumbo frames?
3) Name two SMB client tuning parameters.
4) Why exclude high‑throughput volumes from indexing?
5) What does `oflag=direct` do in `dd` tests?
6) Risk of strict sync on SMB shares?
7) How to measure throughput consistently?
8) One indicator NAS jumbo frames aren’t enabled?
9) Why prefer NVMe or RAID for VM disks?
10) What should a rollback plan include?