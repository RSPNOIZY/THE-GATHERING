# decision-trees/

Branching logic for complex multi-step decisions — rendered as trees
rather than tables.

## Format
- Markdown + Mermaid for diagrams.
- One file per decision domain.

## First trees to author
- [ ] `deploy-sequence.mermaid` — Phase 1 → 4 gating logic
- [ ] `voice-capture.mermaid` — Gabriel's state machine
- [ ] `content-publish.mermaid` — anything external-facing
