#!/usr/bin/env bash
###############################################################################
#  NOIZYLAB — HEALTH ALERTS 🚨
#  "The United Nations of Code"
#  
#  Monitors system health and sends alerts when issues detected.
#  Run via cron or launchd for continuous monitoring.
###############################################################################

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
LOG_DIR="${LOG_DIR:-/tmp/noizylab-health}"
ALERT_LOG="$LOG_DIR/alerts.log"

mkdir -p "$LOG_DIR"

TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')
ALERTS=()

log_alert() {
  local severity="$1"
  local message="$2"
  echo "[$TIMESTAMP] [$severity] $message" >> "$ALERT_LOG"
  ALERTS+=("[$severity] $message")
}

echo "🔍 Running health checks at $TIMESTAMP..."

# ──────────────────────────────────────────────────────────────────────────────
# Check 1: MTU Configuration
# ──────────────────────────────────────────────────────────────────────────────
MTU_EN0=$(ifconfig en0 2>/dev/null | grep -oE 'mtu [0-9]+' | awk '{print $2}' || echo "0")
if [ "$MTU_EN0" != "9000" ]; then
  log_alert "WARNING" "MTU on en0 is $MTU_EN0 (expected 9000)"
fi

# ──────────────────────────────────────────────────────────────────────────────
# Check 2: Worker Health
# ──────────────────────────────────────────────────────────────────────────────
WORKER_URL="https://noizylab.rsplowman.workers.dev"
HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$WORKER_URL" 2>/dev/null || echo "000")
if [ "$HTTP_CODE" != "200" ]; then
  log_alert "CRITICAL" "Cloudflare Worker returned HTTP $HTTP_CODE"
fi

# ──────────────────────────────────────────────────────────────────────────────
# Check 3: Disk Space
# ──────────────────────────────────────────────────────────────────────────────
DISK_USED=$(df -h / | awk 'NR==2 {gsub(/%/,"",$5); print $5}')
if [ "$DISK_USED" -gt 90 ]; then
  log_alert "CRITICAL" "Root disk usage is ${DISK_USED}%"
elif [ "$DISK_USED" -gt 80 ]; then
  log_alert "WARNING" "Root disk usage is ${DISK_USED}%"
fi

# ──────────────────────────────────────────────────────────────────────────────
# Check 4: Volume Availability
# ──────────────────────────────────────────────────────────────────────────────
for VOLUME in "/Volumes/12TB" "/Volumes/4TB"; do
  if [ -d "$VOLUME" ]; then
    # Check if readable
    if ! ls "$VOLUME" &>/dev/null; then
      log_alert "CRITICAL" "Volume $VOLUME is not accessible"
    fi
  fi
done

# ──────────────────────────────────────────────────────────────────────────────
# Check 5: DNS Resolution
# ──────────────────────────────────────────────────────────────────────────────
if ! host github.com &>/dev/null; then
  log_alert "CRITICAL" "DNS resolution failed for github.com"
fi

# ──────────────────────────────────────────────────────────────────────────────
# Check 6: Git Status
# ──────────────────────────────────────────────────────────────────────────────
cd "$SCRIPT_DIR/.."
if [ -d ".git" ]; then
  UNCOMMITTED=$(git status --porcelain 2>/dev/null | wc -l | tr -d ' ')
  if [ "$UNCOMMITTED" -gt 20 ]; then
    log_alert "WARNING" "$UNCOMMITTED uncommitted changes in repo"
  fi
fi

# ──────────────────────────────────────────────────────────────────────────────
# Summary
# ──────────────────────────────────────────────────────────────────────────────
if [ ${#ALERTS[@]} -eq 0 ]; then
  echo "✅ All health checks passed"
  exit 0
else
  echo ""
  echo "╔═══════════════════════════════════════════════════════════════╗"
  echo "║   🚨 HEALTH ALERTS DETECTED                                    ║"
  echo "╚═══════════════════════════════════════════════════════════════╝"
  for alert in "${ALERTS[@]}"; do
    echo "  • $alert"
  done
  echo ""
  echo "📝 Full log: $ALERT_LOG"
  
  # macOS notification (optional)
  if command -v osascript &>/dev/null; then
    ALERT_COUNT=${#ALERTS[@]}
    osascript -e "display notification \"$ALERT_COUNT health issue(s) detected\" with title \"NOIZYLAB Health\" sound name \"Basso\"" 2>/dev/null || true
  fi
  
  exit 1
fi
