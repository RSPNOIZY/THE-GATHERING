{
  "name": "noizystream",
  "version": "1.0.0",
  "description": "NOIZYSTREAM — creator-controlled audio fabric (Dante + WebRTC + AES67)",
  "private": true,
  "scripts": {
    "control": "npx tsx control/server.ts",
    "signaling": "npx tsx signaling/server.ts",
    "dev": "concurrently \"npm run control\" \"npm run signaling\"",
    "build": "npx tsc --noEmit",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.13",
    "concurrently": "^9.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
